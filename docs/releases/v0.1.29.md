# Autopsy Memory v0.1.29

- Prevent embedded RedisLite/Falkor runtime leaks by shutting down CLI-owned clients after every command.
- Gracefully handle worker SIGTERM/SIGINT so upgrade restarts close embedded runtime state before exit.
- Clean stale Homebrew-cellar RedisLite processes during install/doctor cleanup.
- Prefer the active package Python for resident workers before falling back to the legacy Application Support runtime.
- Harden worker Python startup against ambient `PYTHONHOME`, `PYTHONPATH`, and user-site contamination.
